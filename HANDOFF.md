# Termite — Conversation Handoff (v2)

This document gives the next conversation full context to continue development without losing anything. It supersedes the previous handoff, which was written at the end of conversation 1. **Read this one, not conversation 1's — this one corrects a false claim the previous one didn't know about yet.**

---

## Important correction: M1 was NOT actually done

The commit `aa8d928` is titled **"completed m1 and im writing this commit message because i cant do merge conflicts"**. This title is misleading — it is a merge commit resolving a conflict between two branches, and it only touches `LICENSE`, `SECURITY.md`, and a one-line removal in `crates/termite-core/src/types.rs`. **No terminal emulator code exists from that commit.** The user was mid-merge-conflict and the commit message doesn't describe real content.

Verified at the start of this conversation: `crates/termite-terminal/src/lib.rs` was still a 10-line doc-comment stub, and `crates/termite-app/src/lib.rs` was still the M0 placeholder (`Message` is an empty enum, `view()` just prints "Termite — M0 scaffold"). The workspace was, in truth, still at M0. Trust `git show --stat <commit>` over commit messages when auditing what a milestone commit actually contains — this project's git history is not reliable narration.

**This conversation is implementing real M1** (started after discovering the above). See "Current state of M1" below for exactly what's done vs. left.

---

## What this project is

**Termite** — a modern, open-source, native SSH client built entirely in Rust.
- No accounts, no telemetry, no subscriptions, no AI, no cloud.
- Target aesthetic: VS Code / Warp / Obsidian — not PuTTY.
- Cross-platform: Linux, macOS, Windows.
- Philosophy: "It should disappear and let you work."

Full design rationale is in `ARCHITECTURE.md`. Read that first.

---

## Repo state

- **Location:** `~/Documents/code/termite`
- **Git:** `main` branch. Latest commit `aa8d928` (see correction above — treat its message as untrustworthy, not as "M1 done").
- **Remote:** `https://github.com/brass-ape/termite.git` — pushed and up to date as of session start (`git status` showed "up-to-date with origin/main" before this session's uncommitted changes).
- **Uncommitted changes as of writing this handoff** (nothing committed yet this session):
  - `Cargo.toml` (modified) — added `vte`, `portable-pty` to `[workspace.dependencies]`; **also fixed a pre-existing bug**: `uuid` was missing the `serde` feature, which meant `cargo check` failed on `termite-core` (`HostId`/`SessionId` derive `Deserialize` but `Uuid` didn't have serde support enabled) — **this bug predates this session, confirmed via `git stash` + `cargo check` against clean HEAD.** Fixed by changing `uuid = { version = "1", features = ["v4"] }` → `features = ["v4", "serde"]`.
  - `crates/termite-terminal/Cargo.toml` (modified) — added `vte`, `portable-pty`, `thiserror`, `tracing` deps (via workspace).
  - `crates/termite-terminal/src/cell.rs` (new, untracked) — done, see below.
  - `crates/termite-terminal/src/grid.rs` (new, untracked) — done, see below.
  - `Cargo.lock` (new, untracked) — generated by `cargo check`, not yet committed.

No commits have been made this session. The next conversation should commit once the M1 implementation below is finished and verified (see commit workaround section, which may or may not still be needed — check whether `git commit` works normally first before assuming the old sandbox workaround applies).

---

## Workspace structure

```
termite/
├── Cargo.toml                  # Workspace + binary package
├── src/main.rs                 # Entry: calls termite_app::run()
├── ARCHITECTURE.md             # Full design doc — read this
├── CONTRIBUTING.md
├── SECURITY.md
├── LICENSE (MIT)
├── README.md
├── .gitignore
├── deny.toml                   # cargo-deny: licence allow-list, openssl banned
├── .github/workflows/ci.yml    # lint + test (Linux/macOS/Win) + audit + deny
└── crates/
    ├── termite-core/           # Shared types: SessionId, HostProfile, AuthMethod,
    │                           # ConnectionStatus, TermiteError. NO other workspace deps.
    ├── termite-ssh/            # STUB — implemented in M2
    ├── termite-terminal/       # IN PROGRESS (M1) — see below
    ├── termite-storage/        # STUB — implemented in M3
    ├── termite-crypto/         # STUB — implemented in M3
    ├── termite-ui/             # Theme + colour palette. TermiteTheme type.
    └── termite-app/            # Iced 0.13 app skeleton. STILL M0 placeholder — not yet wired to terminal.
```

---

## Current state of M1 (terminal emulator) — what's done vs. left

### Done
- **`crates/termite-terminal/Cargo.toml`** — deps added (`vte`, `portable-pty`, `thiserror`, `tracing`, all via `{ workspace = true }`).
- **`Cargo.toml`** (workspace) — `vte = "0.13"` and `portable-pty = "0.8"` added to `[workspace.dependencies]`. Also fixed the pre-existing `uuid` serde-feature bug (see above) — without this fix, nothing in the workspace compiles.
- **`crates/termite-terminal/src/cell.rs`** — `Cell { ch, fg, bg, attrs }`, `TermColor` (Default/Indexed(u8)/Rgb(u8,u8,u8)), `Attrs` (bold/dim/italic/underline/reverse/strikethrough). Matches the spec in the original handoff.
- **`crates/termite-terminal/src/grid.rs`** — `TerminalGrid` implemented with:
  - primary + alternate screen buffers, `enter_alt_screen`/`leave_alt_screen`
  - cursor state, `save_cursor`/`restore_cursor`
  - `put_char`, `linefeed` (with scrollback push via `VecDeque`, capped at 10,000 lines), `carriage_return`, `backspace`
  - cursor movement: `cursor_up/down/forward/back`, `set_cursor_position` (1-indexed, for CSI H/f)
  - `erase_line`/`erase_display` with `EraseMode` (ToEnd/ToStart/All, mapped from CSI J/K numeric params via `EraseMode::from_param`)
  - SGR pending state: `reset_sgr`, `set_fg`, `set_bg`, `set_attrs` (closure-based mutation)
  - `resize(rows, cols)`, `visible_rows() -> Vec<String>` (for the Iced text renderer), `title()`/`set_title()`
  - **Verified compiles**: `cargo check -p termite-terminal` passes cleanly after the uuid fix.

### Not yet done (pick up here)
1. **`crates/termite-terminal/src/handler.rs`** — `GridHandler` implementing `vte::Perform`. I was mid-way through reading the `vte` 0.13.1 API when interrupted. Key facts gathered (source at `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/vte-0.13.1/src/`):
   - `Perform` trait methods to implement: `print(&mut self, c: char)`, `execute(&mut self, byte: u8)`, `csi_dispatch(&mut self, params: &Params, intermediates: &[u8], ignore: bool, action: char)`, `esc_dispatch(&mut self, intermediates: &[u8], ignore: bool, byte: u8)`, `osc_dispatch(&mut self, params: &[&[u8]], bell_terminated: bool)`. (`hook`/`put`/`unhook` are for DCS — not needed for M1 scope.)
   - `Params` iterates as `&Params` → `ParamsIter<'a>` yielding `&'a [u16]` (one slice per parameter, including its subparameters — e.g. `38;2;r;g;b` arrives as separate top-level params unless colons are used for subparams). For basic SGR handling, iterate `params.iter()` and match on `param[0]` (first element of each slice), consuming extra elements from subsequent iterator items for the multi-part codes (38/48 + 5/2 + values) — **watch out for this**: since `;`-separated params each come back as their own `&[u16]` of length 1 (subparams only appear for `:`-separated codes), the classic pattern is to use a manual index-based walk over a `Vec<u16>` collected from `params.iter().flatten()`, not a simple `for` loop, so you can peek ahead for 38/48 sequences. Check how Alacritty's `ansi.rs` handles this if unsure (not vendored here, but it's a well-known public pattern to search for).
   - Still need to map: CSI `A/B/C/D` → cursor up/down/forward/back; CSI `H`/`f` → `set_cursor_position`; CSI `J` → `erase_display`; CSI `K` → `erase_line`; CSI `m` → SGR (see mapping table in the original M1 spec section below); CSI `?1049h`/`?1049l` → alt screen enter/leave (private mode, check `intermediates == b"?"` and the param value 1049); `ESC 7`/`ESC 8` → save/restore cursor (via `esc_dispatch` with `byte == b'7'`/`b'8'`); OSC `0`/`2` → `set_title` (via `osc_dispatch`, `params[0] == b"0"` or `b"2"`, title text in `params[1]`).
2. **`crates/termite-terminal/src/pty.rs`** — `Pty::spawn(shell, rows, cols)` using `portable-pty`. Not started. Expected shape (standard `portable-pty` usage pattern):
   ```rust
   use portable_pty::{native_pty_system, CommandBuilder, PtySize};
   let pty_system = native_pty_system();
   let pair = pty_system.openpty(PtySize { rows, cols, pixel_width: 0, pixel_height: 0 })?;
   let mut cmd = CommandBuilder::new(shell);
   let child = pair.slave.spawn_command(cmd)?;
   let reader = pair.master.try_clone_reader()?;
   let writer = pair.master.take_writer()?;
   ```
   This has NOT been verified against the actual 0.8 API yet — `portable-pty` source wasn't downloaded/inspected before the interruption (only `vte` and `iced` sources were checked). **Run `cargo doc` or check `~/.cargo/registry/src/*/portable-pty-0.8*` once it's fetched (it will be after the Cargo.toml edit + a `cargo check`) before trusting this snippet.**
3. **`crates/termite-terminal/src/lib.rs`** — currently still the doc-comment-only stub. Needs `pub mod cell; pub mod grid; pub mod handler; pub mod pty;` plus re-exports (`pub use cell::{Cell, TermColor, Attrs}; pub use grid::{TerminalGrid, EraseMode}; pub use handler::GridHandler; pub use pty::Pty;`).
4. **`crates/termite-app/src/lib.rs`** — completely unstarted. Still the M0 placeholder (`Message` is `enum Message {}`, empty). Needs the full integration described in the original M1 plan (state, subscription, update, view) — see below, this part of the spec is unchanged from conversation 1's handoff and still accurate.
5. **`cargo check/build/clippy/fmt` across the whole workspace** — only `termite-terminal` alone has been checked so far. Full workspace check not yet run (will surface issues once `termite-app` is wired up).
6. **Manual verification** — app has not been launched even once this session. No display/GUI testing done yet.

### Iced 0.13 API facts confirmed this session (useful once you get to termite-app)

Verified against the locally cached source at `~/.cargo/registry/src/index.crates.io-1949cf8c6b5b557f/`:
- `iced::application(title, update, view).theme(...).subscription(...).run()` — `.subscription()` takes `impl Fn(&State) -> Subscription<Message>`.
- `iced::time::every(Duration) -> Subscription<Instant>` exists via `iced::time` (re-exported from `iced_futures::backend::default::time`), needs the `tokio` feature on the `iced` dep — already enabled in the workspace `Cargo.toml` (`iced = { version = "0.13", features = ["tokio", "advanced"] }`).
- `iced::keyboard::on_key_press(f: fn(Key, Modifiers) -> Option<Message>) -> Subscription<Message>` — **note: takes a plain `fn` pointer, not a general closure** (no captures allowed). `Message::KeyPressed { key, modifiers }` as a tuple-style constructor function works fine here as long as it's expressed as a fn item, not a closure — a small free function or the enum variant's implicit constructor is the way to do this.
- `iced::keyboard::Key` (from `iced_core::keyboard::key::Key`) is `enum Key<C = SmolStr> { Named(Named), Character(C), Unidentified }`. `Named` has `Enter`, `Tab`, `Backspace`, `Delete`, `Escape`, `ArrowUp/Down/Left/Right`, `Space`, `Home`, `End`, `PageUp/Down`, etc.
- `iced::keyboard::Modifiers` is a `bitflags` struct with `.shift()`, `.control()`, `.alt()`, `.logo()`, `.command()` helper methods.
- `iced::Font::MONOSPACE` exists (`iced_core::font::Font::MONOSPACE`).

### The rest of the original M1 spec is unchanged and still the plan to follow

Everything below (VT sequences to implement, SGR color mapping, key-to-VT mapping, borrow-checker pattern for the parser/grid split, PTY output polling pattern, shell detection, deferred scope) was written in conversation 1's handoff and is still accurate/unimplemented. Copying it forward verbatim so it isn't lost:

**Updated `crates/termite-app/src/lib.rs`** — integrate the terminal:
- `TermiteApp` state: `TerminalGrid`, `vte::Parser`, `OutputBuffer` (`Arc<Mutex<VecDeque<Vec<u8>>>>`), PTY writer
- On init: spawn PTY (default shell via `$SHELL`), start `std::thread` to read PTY → push to `OutputBuffer`
- Subscription: `iced::time::every(16ms)` to poll `OutputBuffer` + `iced::keyboard::on_key_press`
- `Message::PollOutput` — drain buffer, feed bytes to `vte::Parser` → updates grid
- `Message::KeyPressed { key, modifiers }` — convert to VT byte sequence, write to PTY writer
- View: iterate grid rows (`grid.visible_rows()`), render each as `text(row_string).font(Font::MONOSPACE).size(14)`

**Borrow checker pattern for vte parsing** (grid and parser are separate fields):
```rust
pub fn advance(&mut self, bytes: &[u8]) {
    let parser = &mut self.parser;   // split borrow
    let grid   = &mut self.grid;     // split borrow — Rust allows this for separate fields
    let mut handler = GridHandler { grid };
    for &byte in bytes {
        parser.advance(&mut handler, byte);
    }
}
```

**PTY output polling pattern** (avoids complex Iced subscription wiring):
```rust
// std::thread (not tokio) reads PTY → pushes to shared buffer
// Iced timer subscription drains buffer every 16ms
fn subscription(app: &TermiteApp) -> iced::Subscription<Message> {
    use std::time::Duration;
    iced::Subscription::batch([
        iced::time::every(Duration::from_millis(16)).map(|_| Message::PollOutput),
        iced::keyboard::on_key_press(|key, mods| Some(Message::KeyPressed { key, mods })),
    ])
}
```

**Shell detection:**
```rust
let shell = std::env::var("SHELL").unwrap_or_else(|_| "/bin/bash".to_string());
// Windows: "cmd.exe" — handle in M4 with proper platform detection
```

**VT sequences the handler MUST implement for basic usability:**
- `print(c)` → `grid.put_char(c)`
- `execute(0x0a)` LF → `grid.linefeed()`
- `execute(0x0d)` CR → `grid.carriage_return()`
- `execute(0x08)` BS → `grid.backspace()`
- CSI A/B/C/D — cursor up/down/forward/back
- CSI H/f — cursor position (row;col)
- CSI J — erase display (0/1/2)
- CSI K — erase line (0/1/2)
- CSI m — SGR (colors + bold/italic/underline/reverse)
- CSI ?1049h/l — enter/leave alternate screen
- ESC 7/8 — save/restore cursor
- OSC 0/2 — set terminal title

**SGR color mapping (CSI m):**
- 0 → reset all
- 1/2/3/4/7/9 → bold/dim/italic/underline/reverse/strikethrough
- 30-37 → fg Indexed(0-7), 90-97 → fg Indexed(8-15) (bright)
- 40-47 → bg Indexed(0-7), 100-107 → bg Indexed(8-15)
- 38;5;n → fg Indexed(n) (256-color)
- 38;2;r;g;b → fg Rgb(r,g,b) (true color)
- 39 → fg Default, 49 → bg Default

**Key-to-VT mapping (essential keys):**
```
Enter → \r
Backspace → \x7f
Tab → \t
Escape → \x1b
ArrowUp → \x1b[A
ArrowDown → \x1b[B
ArrowRight → \x1b[C
ArrowLeft → \x1b[D
Ctrl+<alpha> → byte (char - 'a' + 1)
```

### What M1 does NOT need to do (deferred)
- Mouse reporting (M6)
- True colour in Iced renderer — just render text as monochrome for M1, colours in M6 (note: the grid already tracks fg/bg/attrs per cell, so this is purely a "don't bother wiring it into the view yet" deferral, not a grid limitation)
- Wide character support (CJK/emoji) — treat all chars as width 1 for M1
- Scrollback UI (M6)
- Split panes (M6)
- Windows ConPTY testing (M4)

---

## Key architectural decisions (already locked in)

| Decision | Choice | Rationale |
|----------|--------|-----------|
| UI framework | **Iced 0.13** | Pure Rust, GPU (wgpu), TEA architecture, MIT |
| SSH library | **russh** | Pure Rust SSH-2, no FFI, MIT |
| VT parsing | **vte 0.13** | Used by Alacritty, Apache-2.0 |
| PTY | **portable-pty 0.8** | Cross-platform, MIT, from WezTerm |
| Async runtime | **tokio 1** | Standard |
| Key formats | **ssh-key 0.6** | RustCrypto, pure Rust |
| Credential storage | **keyring 2** | OS keychain: macOS/Win/Linux |
| Secret memory | **secrecy 0.10 + zeroize 1** | Mandatory for all secret types |
| Config format | **toml 0.8 + serde 1** | Human-editable |
| Error handling | **thiserror 2** (libs) + **anyhow 1** (app) | Standard |
| Logging | **tracing 0.1** | Structured, async-aware |

See ARCHITECTURE.md §4 for full crate justifications.

---

## Dependency graph between crates

```
termite-app
  ├── termite-ui      → termite-core, iced
  ├── termite-ssh     → termite-core, termite-crypto
  ├── termite-terminal → termite-core
  ├── termite-storage → termite-core
  └── termite-crypto  → termite-core
termite-core          → (no workspace deps)
```

This is enforced at the compiler level. Nothing leaks across layers.

---

## Security invariants (never break these)

1. Secrets (`passwords`, `key material`) are ALWAYS wrapped in `secrecy::SecretString` or `secrecy::SecretVec<u8>` — never plain `String` or `Vec<u8>`.
2. `zeroize` zeroes memory on drop. All secret types implement `ZeroizeOnDrop`.
3. Host key verification is MANDATORY. Changed keys produce a prominent warning, never silent accept.
4. Passwords and key passphrases are stored in the OS keychain (`keyring`), NEVER in config files on disk.
5. Secrets NEVER appear in `tracing` log output. `secrecy` enforces this via its `Debug` impl (`[REDACTED]`).

(None of this is touched by M1 — no secrets flow through the terminal emulator.)

---

## Commit conventions

Conventional Commits format: `<type>(<scope>): <description>`

Types: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `perf`, `security`
Scopes: `core`, `ssh`, `terminal`, `storage`, `crypto`, `ui`, `app`

Examples:
```
feat(terminal): implement ANSI 256-colour support
fix(ssh): handle keepalive timeout during reconnect
security(crypto): zeroize key material on parse error
```

Suggested commit for the `uuid` serde fix specifically, if split out from the rest of M1: `fix(core): enable uuid serde feature so HostId/SessionId deserialize`.

### Git commit workaround (may be stale — verify first)

The previous handoff documented a sandbox workaround for git lock file issues (`unlink` blocked, requiring `GIT_INDEX_FILE=/tmp/... git add` + plumbing commands). This was written for a different sandbox environment (`/sessions/admiring-relaxed-gates/mnt/termite`). **This conversation's environment is a normal local checkout at `~/Documents/code/termite` with a working `git status`/`git stash` — the lock-file workaround has not been tested and may not be needed here.** Try a normal `git commit` first; only fall back to the plumbing workaround if it actually fails with a lock error.

---

## Roadmap — milestone status

| Milestone | Status | Description |
|-----------|--------|-------------|
| **M0** | ✅ Done | Workspace scaffold, CI, window opens |
| **M1** | 🔨 In progress | Local terminal emulator (PTY + VT emulation + Iced rendering) — cell.rs/grid.rs done, handler.rs/pty.rs/app-wiring left (see above) |
| M2 | Pending | SSH core (password auth, known hosts) |
| M3 | Pending | Key auth + credential storage |
| M4 | Pending | Host management UI |
| M5 | Pending | Tabs + multi-session |
| M6 | Pending | Advanced terminal (colours, mouse, split) |
| M7 | Pending | Port forwarding, SFTP |
| M8 | Pending | Command palette, UX polish |
| M9 | Pending | Security review, packaging, public release |

---

## Working in this codebase

```sh
# Check (fast — no linking)
cargo check --workspace

# Build
cargo build

# Test
cargo test --workspace

# Lint
cargo clippy --workspace -- -D warnings

# Dependency audit
cargo audit

# Format
cargo fmt
```

CI runs all of the above on push to `main` across Linux, macOS, and Windows.

**Note:** `cargo check --workspace` will fail until the `uuid` serde fix (already applied, uncommitted) is committed — or rather, it will succeed locally since the fix is already in the working tree, but don't lose it when committing. Double check `git diff Cargo.toml` shows the `serde` feature addition before committing.

---

*Handoff v2 written mid-M1, this conversation. Continue by implementing `handler.rs` (vte::Perform for GridHandler), then `pty.rs`, then wire `termite-app`, then run a full workspace check + manual launch test.*
